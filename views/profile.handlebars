<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{user.name}}'s Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body class="bg-pageBg text-textColor">
    <header>
        <nav class="flex justify-between items-center p-6">
            <a href="/feed" class="bg-actionColor px-4 py-2 rounded-full text-white shadow-md">Feed</a>
            <button class="bg-secondaryAction px-4 py-2 rounded-full text-textColor shadow-md"
                id="logout">Logout</button>
        </nav>
    </header>

    <main>
        <h1>Welcome, {{user.name}}!</h1>
        <div class="profile-info">
            <p><strong>Email:</strong> {{user.email}}</p>
        </div>

        <!-- If you want to list the user's pets as well -->
        <section class="pets-section">
            <h2>{{user.name}}'s Pets</h2>
            {{#if user.pets}}
            <ul class="pets-list">
                {{#each user.pets}}
                <li class="mb-4">
                    <div class="pet-item p-4 bg-white rounded shadow">
                        <img src="{{this.image_url}}" alt="{{this.name}}"
                            class="w-32 h-32 object-cover rounded-full mx-auto">
                        <div class="pet-info mt-2 text-center">
                            <span class="pet-name block font-bold">{{this.name}}</span>
                            <span class="pet-type block italic">{{this.type}}</span>
                            <span class="pet-age block">Age: {{this.age}}</span>
                            <span class="pet-breed block">Breed: {{this.breed}}</span>
                            <span class="pet-weight block">Weight: {{this.weight}} lbs</span>
                            <span class="pet-description block mt-2">{{this.description}}</span>
                        </div>
                    </div>
                </li>
                {{/each}}
            </ul>
            {{else}}
            <p>No pets listed.</p>
            {{/if}}
        </section>

    </main>

    <script src="/js/logout.js"></script>
</body>

</html>