<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{user.name}}'s Profile</title>
    <!-- Add links to CSS files here -->
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <header>
        <!-- Navigation bar here -->
        <nav>
            <button class="no-button" id="logout">Logout</button>
        </nav>
    </header>

    <main>
        <h1>Welcome, {{user.name}}!</h1>
        <div class="profile-info">
            <p><strong>Email:</strong> {{user.email}}</p>
        </div>

        <!-- If you want to list the user's pets as well -->
        <section class="pets-section">
            <h2>{{user.name}}'s Pets</h2>
            {{#if user.pets}}
            <ul class="pets-list">
                {{#each user.pets}}
                <li>
                    <span class="pet-name">{{this.name}}</span> - <span class="pet-type">{{this.type}}</span>
                    <!-- You can add more details here -->
                </li>
                {{/each}}
            </ul>
            {{else}}
            <p>No pets listed.</p>
            {{/if}}
        </section>

    </main>

    <footer>
        <!-- Footer content here -->
        &copy; 2023 FureverFriends. All rights reserved.
    </footer>

    <script src="/js/logout.js"></script>
</body>

</html>
